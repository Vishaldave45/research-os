# ResearchOS Implementation & Architecture Progress Tracker

**Milestone Target:** v0.1 — Traceability Core Foundation  
**Status:** In Active Development (Phase A & C Completed, Phase B & D in Progress)  
**Last Updated:** 2026-08-21  

---

## 🗺️ Migration & Architecture Roadmap

| Milestone / Phase | Focus Area | Status | Deliverables & Verification |
| :--- | :--- | :--- | :--- |
| **Phase A: Single Source of Truth** | Refactor Frontend State Management | **COMPLETE** | Zustand store refactored to manage UI state (filters, layout, selection); all entity and relationship mutations execute authoritative async API calls. DAG cycle detection and ontology validation in `relationshipRules.ts`. |
| **Phase B: Canonical FastAPI + PostgreSQL** | Containerized Backend Architecture | **IN PROGRESS** | SQLAlchemy async models, Pydantic v2 schemas, Alembic migrations, and REST API routers in `/backend`. `backend/Dockerfile` and `docker-compose.yml` configured for multi-container deployment. |
| **Phase C: Backward Traceability Engine** | Decision Provenance & Lineage | **COMPLETE** | Deterministic BFS backward graph traversal (`/api/v1/decisions/{id}/trace` and `traceService.ts`). `TraceEvidenceModal` visualizing grounded reasoning chains from decisions back to papers. |
| **Phase D: Semantic Ontology & Graph Integrity** | Directed Acyclic Graph Invariants | **COMPLETE** | Strict relationship vocabulary matrix (`tests`, `supports`, `refutes`, `motivates`, `addresses`, `cites`, `informs`, `derived_from`). BFS-based cycle prevention prevents invalid loops before link creation. |
| **Phase E: End-to-End Integration Testing** | Full-Stack Verification | **IN PROGRESS** | Verification of complete scientific workflow: Question $\rightarrow$ Paper $\rightarrow$ Gap $\rightarrow$ Hypothesis $\rightarrow$ Experiment $\rightarrow$ Result $\rightarrow$ Decision. Pytest backend suite + Vite/TypeScript build verification. |

---

## 🏛️ Architecture Blueprint

### Target Production Topology (FastAPI + PostgreSQL)
```
[ React 19 Frontend (@xyflow/react) ]
                │
         (HTTP / REST)
                ▼
[ FastAPI Application Server (Uvicorn / Port 8000) ]
        │                       │
 (Domain Services)        (SQLAlchemy Async)
        │                       ▼
 [ GraphService ]       [ PostgreSQL 16 DB (Port 5432) ]
```

### AI Studio Container Preview Topology
```
[ React 19 Frontend (Vite) ]
                │
         (HTTP / REST)
                ▼
[ Express Preview Gateway Server (Port 3000) ]
        │
 [ TraceService + Persistent JSON Store ]
```

---

## 🎯 Verification Checklist

- [x] Strict separation of UI state (Zustand) from domain data persistence.
- [x] Ontology relationship validation table enforced on both frontend and backend.
- [x] Cycle detection utility preventing cycles in the reasoning graph.
- [x] `TraceEvidenceModal` renders complete provenance path for decisions.
- [x] `npm run build` and `npm run lint` pass without errors.
- [x] Dockerfile and Docker Compose configurations ready for PostgreSQL + FastAPI execution.
